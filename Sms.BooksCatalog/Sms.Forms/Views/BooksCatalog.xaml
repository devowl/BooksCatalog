<UserControl x:Class="Sms.Forms.Views.BooksCatalog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary Source="..\Resources\SmsResources.xaml"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Border BorderThickness="0,0,0,2" BorderBrush="Black">
            <Grid>
                <TextBlock Text="Каталог книг" HorizontalAlignment="Left" />
            </Grid>
        </Border>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding BooksCatalog}" Template="{StaticResource WrappingPanel}" ItemTemplate="{StaticResource BooksCatalogTemplate}" />
        <Control Grid.Row="2" Template="{StaticResource BooksListControlTemplate}"/>
    </Grid>
</UserControl>
